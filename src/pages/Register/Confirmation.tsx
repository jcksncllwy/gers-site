import { Box, Typography } from "@mui/material"

export const Confirmation = () => {
    return(
    <Box>
        <Typography variant="h1">Thanks for registering!</Typography>
        <Typography variant="h6">We look forward to seeing you</Typography>
    </Box>
    )
}